#!/usr/bin/awk -f

BEGIN { FS = "\t"; OFS="\t" }\
      {\
        gsub(/[^[:alnum:]]/, "", $12);\
      }
      (    ($9  != 0)\
        && ($10 != 0)\
        && ($11 > 0)\
        && ($12 != "")\
      )\
      {\
        print\
      }
